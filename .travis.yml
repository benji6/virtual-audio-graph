language: node_js
jobs:
  include:
    - stage: release
      script: skip
      before_deploy: npm run build
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
